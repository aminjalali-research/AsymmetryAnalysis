# requirements.txt — kept as a backward-compatibility shim
#
# The canonical dependency declaration is in pyproject.toml (PEP 517).
# To install: `pip install -e .` (preferred) or `pip install -r requirements.txt` (legacy).
# Versions here are pinned floors; pyproject.toml may be more authoritative.

numpy>=1.24
scipy>=1.10
pandas>=2.0
nibabel>=5.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
openpyxl>=3.1
plotly>=5.0
statsmodels>=0.12
